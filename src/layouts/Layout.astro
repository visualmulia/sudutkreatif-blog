---
import '../styles/global.css';

interface Props {
	title: string;
	description?: string;
}

const { title, description = "Inspirasi desain ruang kerja, home office setup, dan gadget produktivitas untuk profesional kreatif." } = Astro.props;
---

<!doctype html>
<html lang="id">
	<head>
		<meta charset="utf-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		
		<!-- SEO Meta Tags -->
		<title>{title}</title>
		<meta name="description" content={description} />
		<meta property="og:title" content={title} />
		<meta property="og:description" content={description} />
		<meta property="og:type" content="website" />
	</head>
	<body>
		<!-- Header Navbar -->
		<header class="header">
			<div class="container header-container">
				<a href="/" class="logo">
					Sudut<span>Kreatif</span>.
				</a>
				<nav class="nav">
					<a href="/" class="nav-link">Home</a>
					<a href="/#about" class="nav-link">Tentang</a>
					<a href="/admin/" class="nav-link btn-cms">CMS Admin</a>
				</nav>
			</div>
		</header>

		<!-- Main Content Slot -->
		<main>
			<slot />
		</main>

		<!-- Footer -->
		<footer class="footer">
			<div class="container footer-container">
				<div class="footer-brand">
					<a href="/" class="logo">Sudut<span>Kreatif</span>.</a>
					<p class="footer-desc">Inspirasi ruang kerja minimalis modern dan panduan home office setup untuk menunjang produktivitas Anda.</p>
				</div>
				<div class="footer-divider"></div>
				<div class="footer-bottom">
					<p>&copy; {new Date().getFullYear()} SudutKreatif. All rights reserved.</p>
					<p>Ternak Blog Project - Auto Publisher System</p>
				</div>
			</div>
		</footer>
	</body>
</html>

<style>
	.header {
		background-color: rgba(246, 244, 240, 0.85);
		backdrop-filter: blur(10px);
		border-bottom: 1px solid var(--border-color);
		position: sticky;
		top: 0;
		z-index: 100;
		padding: 1.25rem 0;
		transition: var(--transition);
	}

	.header-container {
		display: flex;
		justify-content: space-between;
		align-items: center;
	}

	.logo {
		font-family: var(--font-display);
		font-size: 1.5rem;
		font-weight: 800;
		letter-spacing: -0.5px;
	}

	.logo span {
		color: var(--accent);
	}

	.nav {
		display: flex;
		align-items: center;
		gap: 2rem;
	}

	.nav-link {
		font-size: 0.95rem;
		font-weight: 500;
		color: var(--text-secondary);
	}

	.nav-link:hover {
		color: var(--text-primary);
	}

	.btn-cms {
		border: 1px solid var(--accent);
		color: var(--accent);
		padding: 0.4rem 1rem;
		border-radius: 4px;
		font-size: 0.85rem;
	}

	.btn-cms:hover {
		background-color: var(--accent);
		color: #FFFFFF;
	}

	/* Footer Styles */
	.footer {
		background-color: #FFFFFF;
		border-top: 1px solid var(--border-color);
		padding: 4rem 0 2rem 0;
		margin-top: 5rem;
	}

	.footer-container {
		display: flex;
		flex-direction: column;
		gap: 2.5rem;
	}

	.footer-brand {
		max-width: 500px;
		display: flex;
		flex-direction: column;
		gap: 1rem;
	}

	.footer-desc {
		color: var(--text-secondary);
		font-size: 0.95rem;
	}

	.footer-divider {
		height: 1px;
		background-color: var(--border-color);
	}

	.footer-bottom {
		display: flex;
		justify-content: space-between;
		align-items: center;
		flex-wrap: wrap;
		gap: 1rem;
		color: var(--text-secondary);
		font-size: 0.85rem;
	}

	@media (max-width: 600px) {
		.header-container {
			flex-direction: column;
			gap: 1rem;
		}
		.nav {
			gap: 1.5rem;
		}
	}
</style>
